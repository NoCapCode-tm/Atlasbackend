

services:
   backend:
    image: nocapcode/atlas_backend:latest
    env_file:
      - .env
    ports:
      - "5000:5000"
    restart: unless-stopped

   mongo:
    image: mongo
    restart: always
    volumes: 
     - mongo-data:/data/db

volumes: 
  mongo-data:
     

  

